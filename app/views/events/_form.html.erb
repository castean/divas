<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :info %><br />
    <%= f.text_area :info %>
  </div>
  
  <div class="field">
  	<%= f.label :start_at %>
  	<%= f.datepicker :start_at, :dateFormat => "dd-mm-yy" %>
    <!--<%= f.label :start_at %><br />
    <%= f.date_select :start_at %>-->
  </div> 
  <!-- <div class="field">
    <%= f.label :start_at %><br />
    <%= f.text_field :start_at %>
  </div>!-->
    <div class="field">
    <%= f.label :end_at %><br />
    <%= f.date_select :end_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
